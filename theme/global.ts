import { GlobalStyles } from "@chakra-ui/theme-tools"

const styles : GlobalStyles = {
    global: {
        body: {
            bg: "background.500",
        },
        p: {
            color: "white",
        }
    },
}

export default styles